version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.12
        environment:
          GO111MODULE: "ON"

    working_directory: /go/src/github.com/promignis/cwitch
    steps:
      - checkout
      - run: go get
      - run: go build -v
